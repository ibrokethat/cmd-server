---
$schema: "http://json-schema.org/draft-04/schema#"

id: "http://www.yubl.io/schemas/login.api"

type: object

properties:
  access_token:
    type: string

  activated:
    type: boolean

  status:
    type: string
    enum:
      - OK
      - NOT_OK

required:
  - token
  - activated
  - access_token
