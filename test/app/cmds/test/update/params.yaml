---
$schema: "http://json-schema.org/draft-04/schema#"

id: "http://www.cmd-server.com/schemas/test.update.input"

type: object

properties:
  id:
    type: string

  name:
    type: string

required:
  - id
  - name
